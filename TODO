TODO

1. symlinks and backup of original files
2. describe and install dependencies

# DEPENDENCIES
# For nvim lsp
npm install pyright

# For tmux copy to buffer shortcut
    # on wayland
sudo apt install wl-clipboard
    # on xorg
sudo apt install xclip


